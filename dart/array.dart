void main()
{
	var arr=['one','two','three','four','five'];
	print(arr.first);
	print(arr.last);
	print(arr.length);
	print(arr.isEmpty);
	print(arr);
	for(var i=0;i<arr.length;i++)
		print(arr[i]);
}
